@plan

We are starting the REAL NUCS remediation. There is no smoke-test phase.

Before planning, read these files completely:
- `specs/NUCS_PRODUCT_DECISIONS.md`
- `specs/NUCS_REMEDIATION_SPEC.md`
- all relevant root/module `AGENTS.md` files generated by `/init-deep`
- the current repository code and existing tests needed to verify every architectural claim

## Authority and ambiguity
`specs/NUCS_PRODUCT_DECISIONS.md` contains product decisions already made by the owner. They are authoritative.

DO NOT reopen them, replace them with simpler behavior, infer different UX because implementation is easier, weaken conservative matching/dedup semantics, or convert technical difficulty into a product decision.

Make technical implementation decisions yourself. Ask me only for a genuinely NEW ambiguity that is not resolved by the product-decision file and would materially change user-visible behavior. Do not ask questions answerable by repository inspection or technical judgment.

## Planning mode
Use OMO high-accuracy planning automatically. Before finalizing:
- explore the repository;
- consult Metis for hidden gaps, scope-creep risks, missing acceptance criteria and AI failure modes;
- submit the plan to both Momus and Oracle;
- fix blocking issues and resubmit until both approve.
Do not ask me whether to enable high-accuracy review.

## Core strategy
This is a remediation of the existing implementation, NOT a rewrite. Work only on the dedicated remediation branch `remediation/nucs` (in the dedicated remediation worktree). Do not plan a second implementation branch or per-phase branches. Destructive reset of user data is forbidden. The final merge to main remains HUMAN-ONLY; automated delivery of verified work to `origin/remediation/nucs` is required.

## Git delivery policy
Plan the following lifecycle for EVERY coherent top-level implementation task:

implementation → targeted verification → inspect diff → atomic commit → normal push to `origin/remediation/nucs`

- One verified coherent top-level plan task = one atomic commit. Never commit changes known to fail their acceptance criteria; never one giant commit for the whole remediation; never a commit per tiny edit.
- Use meaningful conventional-style messages: `feat(identity): ...`, `fix(discovery): ...`, `fix(scan): ...`, `test(dedup): ...`, `refactor(discovery): ...`.
- Automatic push is REQUIRED: before pushing verify `git branch --show-current` returns `remediation/nucs`. If no upstream exists, establish it safely with `git push -u origin remediation/nucs`; afterward use ordinary `git push`.
- If a push fails (authentication, missing remote, non-fast-forward history, branch protection, other Git safety condition): preserve local commits, report the blocker, and NEVER recover with force push. A temporary push failure must not discard implemented verified work.
- Every major phase ends with a Git delivery gate: phase verification passed; blocking review findings resolved; no accidental unrelated files included; verified commits created and pushed; local branch and normal upstream state checked. The remote branch must represent the latest verified committed checkpoint.
- NEVER plan: merging `remediation/nucs` into main, pushing main, force push (`git push --force` or `--force-with-lease`), destructive reset of published remediation history, rewriting published branch history, deleting the remote remediation branch, publishing packages/releases to external registries, or deploying to production.

## Required phase order
Preserve these boundaries and dependencies:
0. Baseline and safety
1. Canonical artist/release external identities
2. Conservative matching, homonyms, split handling and identity-management UX
3. Apple-first identity-driven discovery and conservative edition deduplication
4. Filter-aware discovery memory and performance
5. Upcoming releases and persisted idempotent notifications
6. Scan lifecycle, cancellation, truthful progress, refresh/cache coherence and reset safety
7. Errors read/unread, diagnostic Markdown, login transport/error UX
8. UI/product coherence cleanup
9. Full deterministic regression, migration/security/integration audit
10. Live-provider validation of the original reported examples and final audit

## Task sizing
Split work into focused dependency-ordered tasks small enough for delegated execution. Prefer schema/migration, backfill, service/domain logic, API contract, backend integration, frontend contract, frontend UX, deterministic regression coverage, and integration verification as separate coherent units when appropriate.

## Mandatory lifecycle per phase
For EACH architectural phase explicitly plan:
1. focused implementation tasks
2. targeted deterministic verification
3. independent/adversarial review checkpoint
4. bug-fix task for confirmed findings
5. fresh re-verification / phase gate

The review must try to falsify correctness and inspect surrounding code. Severity: Critical / High / Medium / Low. A phase cannot pass with unresolved Critical/High or Medium findings affecting correctness, data/migration safety, security, concurrency safety, or acceptance criteria.

## Verification infrastructure
Inspect and use the existing NUCS tooling:
Backend: pytest, pytest-asyncio, Ruff, Alembic.
Frontend: existing `npm run build` (TypeScript + Vite).
Browser/integration: existing `e2e/` Puppeteer + Chrome headless harness.
Add a deterministic remediation E2E scenario rather than relying on live providers for CI-style acceptance. Do NOT require an Amp-only dev-browser skill. UI work requires deterministic browser/API verification, not compilation alone.

Mock/fixture external provider APIs for deterministic tests. Live Apple/Deezer/MusicBrainz/etc. checks belong only in explicit final live-validation tasks.

## Migration/data rules
Protect upgrades from the current NUCS schema. For schema changes require clean DB migration, representative legacy/current DB migration, backfill verification, constraints/indexes, cascade/reset coverage, preservation of state, and no library reset shortcut. Prefer expand -> backfill/migrate -> switch reads/writes -> retire legacy authority. Avoid permanent dual-write split-brain.

## SQLite/network rule
Never hold a SQLite write transaction across slow external-provider awaits. Preserve short transactions. Remove redundant work before adding concurrency; only add bounded concurrency if measurements still justify it.

## Identity/matching rule
False-positive identity is worse than unresolved identity. Daily discovery must rely on persisted identities rather than repeated provider name searches. Never auto-pick a homonym because it is first/highest provider result.

## Dedup rule
Cross-provider merge only for the same EDITION with strong evidence. Preserve semantic version terms such as Deluxe, Remastered, Extended, Remix, Anniversary, Live, Acoustic. If uncertain, keep separate.

## Upcoming rule
Upcoming and Released are views over the SAME canonical Release row. No row-moving/promotion copy job. Use configured timezone. Persist idempotent notification events.

## Cancellation rule
Library cancellation must be cooperative because the file scan runs in a worker thread. Cancelling an asyncio to_thread wrapper is not sufficient. Preserve already committed valid work. Final run state is cancelled, not error.

## Frontend cache rule
Fix stale post-scan UI via running->idle completion detection and affected React Query invalidation. Do not poll the entire Feed forever.

## Security
Preserve and regression-test sessions/auth, CSRF/origin, login rate limits, trusted proxies, provider URL validation/SSRF boundaries, and secret scrubbing. Diagnostic reports must not leak secrets.

## Persistent OMO knowledge
Use OMO task state and notepads. Keep reusable engineering knowledge in AGENTS.md/learnings, technical rationale in decisions.md, blockers in issues/problems, and actual checks in verification.md. Do not convert newly invented product behavior into AGENTS.md/notepad decisions.

## Original live cases
Final live-provider validation using disposable/test data must cover:
- Pepp 'O Red
- Enzo Dong
- Young Donghito
- Manu T4L
- La traviesa malcría
- PiKi homonym contamination
- Axwell — Whatever Turns You On cross-provider dedup
- an Enzo Dong featured-release case when available
If upstream data is bad/changed, classify it as upstream ambiguity; do not weaken matching rules to force PASS.

## Versioning and release policy
NUCS versions as `APP_VERSION` in `backend/app/main.py` (mirrored in `frontend/package.json` and `e2e/package.json`; git tag `v1.0.0` exists). There is NO changelog convention. The existing `vX.Y.Z` mechanism is authoritative — do NOT introduce a second versioning mechanism and do NOT bump the version per phase.

Plan ONE final release/version gate AFTER all of:
- all remediation implementation phases;
- all phase reviews and fixes;
- full deterministic regression;
- migration compatibility verification;
- security verification;
- deterministic remediation E2E;
- live-provider validation;
- final independent cross-phase audit.

Only after every mandatory gate passes, plan exactly:
1. determine the next semantic version (PATCH = backward-compatible bug fixes; MINOR = backward-compatible new functionality/materially expanded behavior; MAJOR = intentional compatibility-breaking changes);
2. update every authoritative version location consistently (`APP_VERSION` in `backend/app/main.py`, `version` in `frontend/package.json`, `version` in `e2e/package.json`, and any version-asserting tests);
3. finalize release notes (fixed bugs, user-visible changes, new features, migration/upgrade notes, known limitations) under the project's simplest repository-consistent mechanism — no marketing copy;
4. rerun any verification affected by version metadata;
5. create commit `chore(release): vX.Y.Z`;
6. create the annotated tag `vX.Y.Z` — never overwrite/reuse an existing git tag;
7. push `remediation/nucs`;
8. push the new tag.

The final tag does NOT authorize merging to main.

## Final audit
End with a fresh independent audit covering identity migration vs matching/discovery; release identities vs dedup; ReleaseArtist roles vs highlighting; Upcoming vs notification idempotency; cancellation vs cache invalidation; Errors unread vs navbar; login transport vs authenticated 401; Reset Library vs new tables; backup vs new persistence; stale legacy authority; security/redaction; full deterministic tests/build/E2E.

The completed branch must be ready for HUMAN review: all verified commits pushed to `origin/remediation/nucs`, never merge into main, never force push. Do not merge into main.

Generate the complete executable OMO plan now.
